---
phase: 11-polish-reliability
plan: 05
type: execute
wave: 3
depends_on: [11-01, 11-02, 11-03]
files_modified: []
autonomous: true
requirements: [MEM-01, MEM-02, MEM-03, MEM-04]
user_setup: []

must_haves:
  truths:
    - "Memory stores only important conversations, not everything"
    - "Project-specific knowledge is automatically tagged"
    - "Personal notes are prioritized in retrieval"
    - "Memory can be queried with filters (date, project, type)"
  artifacts:
    - path: "memory/filtered_store.py"
      provides: "Filtered vector memory with importance scoring"
      exports: ["FilteredMemory", "MemoryFilter", "MemoryEntry"]
    - path: "memory/importance.py"
      provides: "Importance scoring for memory entries"
      exports: ["ImportanceScorer", "calculate_importance"]
  key_links:
    - from: "memory/filtered_store.py"
      to: "memory/chroma_store.py"
      via: "FilteredMemory wraps ChromaDB"
      pattern: "super.*ChromaDB.*FilteredMemory"
    - from: "memory/importance.py"
      to: "memory/filtered_store.py"
      via: "ImportanceScorer.score()"
      pattern: "filtered_store.*importance"
---

<objective>
Build Filtered Vector Memory - store only important conversations and knowledge.

Purpose: Instead of storing everything, JARVIS should filter by importance. Project knowledge, personal notes, and significant conversations get stored. This prevents memory bloat and improves retrieval quality.

Output: memory/filtered_store.py, memory/importance.py
</objective>

<context>
@memory/chroma_store.py (existing ChromaDB integration)
@memory/MemoryManager.py (existing memory manager)
@context/project_detector.py (for project tagging)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create importance scoring system</name>
  <files>memory/importance.py</files>
  <action>
Create memory/importance.py with:
- MemoryEntryType enum: CONVERSATION, FACT, CODE, PROJECT, NOTE, DECISION
- ImportanceScorer class:
  - score(content: str, entry_type: MemoryEntryType, metadata: dict) -> float (0-1):
  - Scoring factors:
    - Explicit importance keywords: "important", "remember", "don't forget", "key", "critical" (+0.3)
    - Question marks (indicates learning) (+0.1)
    - Code blocks (+0.2 for CODE type)
    - Decision language: "decided", "agreed", "will use", "choose" (+0.3 for DECISION type)
    - Project references: current project name (+0.2)
    - Personal notes markers: "note:", "remember that" (+0.3)
    - Length: prefer medium (50-500 chars), penalize very short (<20) or very long (>2000)
  - calculate_importance(content, entry_type, project_name) -> float
  - is_worth_storing(score_threshold=0.3) -> bool
  - Metadata extraction: extract dates, entities, project names
  </action>
  <verify>python -c "
from memory.importance import ImportanceScorer, MemoryEntryType
scorer = ImportanceScorer()
score = scorer.score('Remember that we decided to use PostgreSQL', MemoryEntryType.DECISION, 'myproject')
print(f'Importance: {score}')
"</verify>
  <done>Importance scoring assigns 0-1 scores to content</done>
</task>

<task type="auto">
  <name>Task 2: Build filtered memory store</name>
  <files>memory/filtered_store.py</files>
  <action>
Create memory/filtered_store.py with:
- MemoryFilter dataclass:
  - entry_types: list[MemoryEntryType] | None (None = all)
  - projects: list[str] | None
  - date_from: datetime | None
  - date_to: datetime | None
  - min_importance: float = 0.0
  - limit: int = 10
- MemoryEntry dataclass:
  - id: str
  - content: str
  - entry_type: MemoryEntryType
  - importance: float
  - project: str | None
  - created_at: datetime
  - metadata: dict
- FilteredMemory class (wraps existing ChromaDB):
  - __init__(chroma_client, importance_scorer)
  - add(content: str, entry_type: MemoryEntryType, metadata: dict) -> str:
    - Calculate importance score
    - Only store if score >= threshold (default 0.3)
    - Auto-tag with current project from context
  - search(query: str, filter: MemoryFilter | None = None) -> list[MemoryEntry]:
    - Search vector store
    - Apply filters
    - Sort by relevance + importance
    - Return top N results
  - get_by_project(project: str) -> list[MemoryEntry]
  - get_recent(limit=10) -> list[MemoryEntry]
  - delete(entry_id: str) -> bool
  - get_stats() -> dict: total, by_type, by_project, average_importance
  </action>
  <verify>python -c "
from memory.filtered_store import FilteredMemory, MemoryFilter
# Test filter creation
f = FilteredMemory(None, None)
results = f.search('API design', MemoryFilter(min_importance=0.5))
print(f'Found: {len(results)} entries')
"</verify>
  <done>FilteredMemory stores and retrieves with importance filtering</done>
</task>

<task type="auto">
  <name>Task 3: Add smart auto-tagging</name>
  <files>memory/filtered_store.py</files>
  <action>
Enhance FilteredMemory with auto-tagging:
- Auto-detect entry type from content:
  - Contains code blocks → CODE
  - Contains "we decided" / "agreed" → DECISION
  - Contains "note:" / "remember" → NOTE
  - References current project → PROJECT
  - Default → CONVERSATION
- Auto-detect project from context (use project_detector)
- Auto-extract entities (names, dates, technologies)
- Store metadata with each entry:
  - {project, entities, detected_type, word_count, has_code}
- Add bulk_import entries with automatic type detection
  </action>
  <verify>python -c "
from memory.filtered_store import FilteredMemory
fm = FilteredMemory(None, None)
# Auto-tagging would be tested during add()
print('Auto-tagging configured')
"</verify>
  <done>Memory entries are auto-tagged with type and project</done>
</task>

<task type="auto">
  <name>Task 4: Add memory API endpoints</name>
  <files>ui/app.py</files>
  <action>
Add memory endpoints to FastAPI:
- POST /api/memory/add
  - Body: {content, entry_type, metadata}
  - Returns: {entry_id, importance_score}
- GET /api/memory/search
  - Query: q, entry_type, project, min_importance, limit
  - Returns: list of MemoryEntry
- GET /api/memory/{entry_id}
  - Returns: MemoryEntry details
- DELETE /api/memory/{entry_id}
- GET /api/memory/stats
  - Returns: memory statistics
- GET /api/memory/recent
  - Query: limit
  - Returns: recent memory entries
- POST /api/memory/clear
  - Body: {project} (optional, clears all or project-specific)
- Integrate with context/project_detector for auto-tagging
  </action>
  <verify>python -c "
from ui.app import app
routes = [r.path for r in app.routes if 'memory' in r.path]
print(f'Memory routes: {routes}')
"</verify>
  <done>Memory API endpoints are available</done>
</task>

<task type="auto">
  <name>Task 5: Integrate with MemoryManager</name>
  <files>memory/MemoryManager.py</files>
  <action>
Update MemoryManager to use filtered memory:
- Add optional filtered_memory parameter
- When adding conversations, use importance scoring
- When retrieving context, use filters
- Maintain backward compatibility:
  - Old add() still works
  - Filtered add() is new option
- Add get_important_context() method:
  - Get high-importance memories for current project
  - Prioritize decisions and facts
  - Limit to 5 most relevant
- Update prompt context injection to use filtered memory
  </action>
  <verify>python -c "
from memory.MemoryManager import MemoryManager
mm = MemoryManager()
# Check if new methods exist
print(f'has get_important_context: {hasattr(mm, \"get_important_context\")}')
"</verify>
  <done>MemoryManager integrates filtered memory</done>
</task>

</tasks>

<verification>
- [ ] Importance scoring works (0-1 scale)
- [ ] Only important entries are stored (threshold 0.3)
- [ ] Memory can be filtered by type, project, date
- [ ] Auto-tagging detects entry types
- [ ] Memory stats are available via API
- [ ] Retrieval prioritizes importance + relevance
</verification>

<success_criteria>
Filtered Memory complete when:
- Memory stores only important conversations
- Project-specific knowledge is auto-tagged
- Memory can be queried with filters
- API endpoints work correctly
- Retrieval quality is improved (more relevant results)
- All existing tests pass
</success_criteria>

<output>
After completion, create .planning/phases/11-polish-reliability/11-05-SUMMARY.md
</output>
