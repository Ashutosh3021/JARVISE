---
phase: 11-polish-reliability
plan: 04
type: execute
wave: 3
depends_on: [11-01, 11-02, 11-03]
files_modified: []
autonomous: true
requirements: [CHN-01, CHN-02, CHN-03, CHN-04]
user_setup: []

must_haves:
  truths:
    - "JARVIS can execute multi-step workflows (max 5 steps)"
    - "Each step's output feeds into the next step"
    - "User can see chain progress in real-time"
    - "Chains can be interrupted at any step"
  artifacts:
    - path: "brain/chains.py"
      provides: "Multi-step task chain execution"
      exports: ["TaskChain", "ChainStep", "ChainResult", "ChainStatus"]
  key_links:
    - from: "brain/chains.py"
      to: "brain/agent.py"
      via: "chain.execute() calls tools"
      pattern: "agent.run.*chain"
    - from: "brain/chains.py"
      to: "ui/websocket"
      via: "chain progress events"
      pattern: "websocket.*chain.*progress"
---

<objective>
Build Task Chains - multi-step workflows with up to 5 steps.

Purpose: Enable JARVIS to execute sequences like "search web → scrape pages → summarize → save notes". Each step's output feeds into the next.

Output: brain/chains.py with TaskChain class
</objective>

<context>
@brain/agent.py (tool execution)
@brain/tools.py (individual tools)
@ui/app.py (WebSocket for progress)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define chain data structures</name>
  <files>brain/chains.py</files>
  <action>
Create brain/chains.py with:
- ChainStatus enum: PENDING, RUNNING, COMPLETED, FAILED, INTERRUPTED
- ChainStep dataclass:
  - step_number: int
  - action: str (tool name or LLM request)
  - input: str (from previous step or user)
  - output: str | None
  - status: ChainStatus
  - error: str | None
  - duration_ms: int
- ChainResult dataclass:
  - chain_id: str
  - steps: list[ChainStep]
  - status: ChainStatus
  - total_duration_ms: int
  - final_output: str
- TaskChain class (interface definition)
  </action>
  <verify>python -c "from brain.chains import TaskChain, ChainStep, ChainResult, ChainStatus; print('OK')"</verify>
  <done>Chain data structures defined</done>
</task>

<task type="auto">
  <name>Task 2: Build TaskChain execution engine</name>
  <files>brain/chains.py</files>
  <action>
Implement TaskChain class:
- __init__(agent, tool_registry, max_steps=5)
- Define built-in chain templates:
  - "research_and_summarize": [web_search, scrape_content, llm_summarize, save_to_file]
  - "code_review": [list_files, read_code, llm_review, create_review_file]
  - "find_and_replace": [search_files, read_files, llm_replace, save_files]
- parse_chain_request(user_input: str) -> list[ChainStep]:
  - Use LLM to break down complex request into steps
  - Validate steps don't exceed max_steps
  - Return list of ChainStep objects
- execute_chain(steps: list[ChainStep], progress_callback=None) -> ChainResult:
  - Execute each step sequentially
  - Pass output of step N as input to step N+1
  - Track status, duration, errors
  - Allow interruption between steps
  - Return ChainResult with all details
- interrupt(): Stop chain execution at next step boundary
  </action>
  <verify>python -c "
from brain.chains import TaskChain
from brain.agent import ReActAgent
from brain.tools import ToolRegistry
chain = TaskChain(ReActAgent(), ToolRegistry())
templates = chain.list_templates()
print(f'Templates: {templates}')
"</verify>
  <done>TaskChain can parse and execute multi-step workflows</done>
</task>

<task type="auto">
  <name>Task 3: Implement chain progress streaming</name>
  <files>brain/chains.py</files>
  <action>
Add streaming support to TaskChain:
- progress_callback(signals: dict):
  - "step_start": step_number, action
  - "step_progress": step_number, partial_output
  - "step_complete": step_number, output, duration_ms
  - "step_error": step_number, error
  - "chain_complete": final_output, total_duration
- Execute steps asynchronously to allow progress updates
- Use generators for step-by-step output
- Integrate with WebSocket to stream progress to UI
- Add /api/chains/execute endpoint for chain execution
  </action>
  <verify>python -c "
import asyncio
from brain.chains import TaskChain
from brain.agent import ReActAgent
from brain.tools import ToolRegistry

async def test():
    chain = TaskChain(ReActAgent(), ToolRegistry())
    # Test with simple chain
    steps = [
        {'action': 'get_time', 'input': ''},
    ]
    result = await chain.execute_chain_async(steps)
    print(f'Status: {result.status}')

asyncio.run(test())
"</verify>
  <done>Chain progress streams to UI via WebSocket</done>
</task>

<task type="auto">
  <name>Task 4: Add chain management API</name>
  <files>ui/app.py</files>
  <action>
Add chain endpoints to FastAPI:
- POST /api/chains/execute
  - Body: {user_input: str} or {steps: list[dict]}
  - Returns: chain_id for tracking
- GET /api/chains/{chain_id}/status
  - Returns: ChainResult with current status
- POST /api/chains/{chain_id}/interrupt
  - Interrupts running chain
- GET /api/chains/templates
  - Returns: available chain templates
- POST /api/chains/templates
  - Body: {name, steps: list[dict]}
  - Save custom chain template
- Store chain history in memory (last 20 chains)
- Add chain status to /api/stats/current
  </action>
  <verify>python -c "
# Verify chain API structure
from ui.app import app
routes = [r.path for r in app.routes if 'chain' in r.path]
print(f'Chain routes: {routes}')
"</verify>
  <done>Chain management API endpoints exist</done>
</task>

<task type="auto">
  <name>Task 5: Add chain commands to router</name>
  <files>brain/router.py</files>
  <action>
Update CommandRouter to detect chain requests:
- Add chain trigger patterns:
  - "research * and summarize"
  - "find * and replace"
  - "search * and save"
  - "look up * then *"
  - "do X, then Y, then Z"
- If chain detected:
  - Return route_type = CHAIN with chain_name or parsed steps
- Integrate TaskChain into router:
  - router.execute_chain(steps) -> ChainResult
- Add to UI: Chain execution button/indicator
  </action>
  <verify>python -c "
from brain.router import CommandRouter
router = CommandRouter()
result = router.route('research AI and summarize')
print(f'Route: {result.route_type}')
"</verify>
  <done>Router detects and routes chain requests</done>
</task>

</tasks>

<verification>
- [ ] Chain templates are predefined
- [ ] Custom chains can be created
- [ ] Progress streams to UI in real-time
- [ ] Chains can be interrupted
- [ ] Max 5 steps enforced
- [ ] Chain results are stored in history
</verification>

<success_criteria>
Task Chains complete when:
- Multi-step workflows execute correctly
- Progress streams to UI via WebSocket
- Chains can be interrupted
- Chain templates are available
- Router detects chain requests
- All existing tests pass
</success_criteria>

<output>
After completion, create .planning/phases/11-polish-reliability/11-04-SUMMARY.md
</output>
