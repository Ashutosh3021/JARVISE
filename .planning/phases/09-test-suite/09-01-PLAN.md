---
phase: 09-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_hardware.py
  - tests/test_config.py
  - tests/test_memory.py
  - tests/test_agent.py
  - tests/conftest.py
autonomous: true
requirements: [TS-01, TS-02, TS-03, TS-04, TS-05, TS-06, TS-07, TS-08, TS-09]
must_haves:
  truths:
    - Unit tests pass for hardware detection module
    - Unit tests pass for config module
    - Unit tests pass for memory system
    - Integration test passes: text input → agent → response
  artifacts:
    - path: tests/test_hardware.py
      provides: Hardware detection unit tests
      contains: test_detect_hardware, test_cpu_cores, test_gpu_detection
    - path: tests/test_config.py
      provides: Config module unit tests
      contains: test_load_config, test_profile_selection
    - path: tests/test_memory.py
      provides: Memory system unit tests
      contains: test_memory_manager_init, test_vector_store
    - path: tests/test_integration.py
      provides: Integration test
      contains: test_text_to_response_pipeline
  key_links:
    - from: tests/
      to: core/hardware.py
      via: import detect_hardware
    - from: tests/
      to: core/config.py
      via: import load_config
    - from: tests/
      to: memory/
      via: import MemoryManager
    - from: tests/
      to: brain/agent.py
      via: import ReActAgent
---

<objective>
Create comprehensive test suite for JARVIS components: unit tests for hardware, config, memory, and integration test for the full agent pipeline.
</objective>

<context>
@core/hardware.py - Hardware detection module
@core/config.py - Configuration module
@memory/ - Memory system
@brain/agent.py - ReAct agent

Phase 9 Requirements:
- TS-01: Unit tests for hardware detection
- TS-02: Unit tests for config module
- TS-03: Unit tests for memory system
- TS-04: Integration test: text input → agent → response
- TS-05-TS-09: Additional tests as needed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test fixtures and conftest.py</name>
  <files>tests/conftest.py</files>
  <action>
    Create pytest fixtures:
    
    1. Mock hardware info fixture
    2. Mock config fixture
    3. Temp directory fixture for tests
    4. Mock Ollama client fixture
    
    ```python
    import pytest
    from unittest.mock import MagicMock, patch
    
    @pytest.fixture
    def mock_hardware():
        return MagicMock(
            cpu_physical_cores=6,
            cpu_logical_cores=12,
            vram_total_mb=0,
            gpu_name="Unknown",
            has_nvidia=False,
            has_amd=True
        )
    
    @pytest.fixture
    def temp_data_dir(tmp_path):
        data_dir = tmp_path / "data"
        data_dir.mkdir()
        return data_dir
    ```
  </action>
  <verify>
    <automated>python -c "import pytest; from tests.conftest import *; print('Fixtures OK')"</automated>
  </verify>
  <done>
    - pytest fixtures available
    - Mock objects configured
  </done>
</task>

<task type="auto">
  <name>Task 2: Create hardware detection unit tests</name>
  <files>tests/test_hardware.py</files>
  <action>
    Create tests/test_hardware.py:
    
    1. test_detect_hardware_returns_info
    2. test_cpu_cores_detected
    3. test_vram_detected
    4. test_gpu_name_reported
    5. test_has_nvidia_flag
    6. test_has_amd_flag
    
    Use mocking for GPU detection to avoid dependency on actual hardware.
  </action>
  <verify>
    <automated>python -m pytest tests/test_hardware.py -v 2>&1 | head -20</automated>
  </verify>
  <done>
    - Hardware tests exist
    - Tests pass with mocking
  </done>
</task>

<task type="auto">
  <name>Task 3: Create config module unit tests</name>
  <files>tests/test_config.py</files>
  <action>
    Create tests/test_config.py:
    
    1. test_load_config_defaults
    2. test_profile_selection_cpu
    3. test_profile_selection_low_gpu
    4. test_profile_selection_mid_gpu
    5. test_profile_selection_high_gpu
    6. test_config_from_env
  </action>
  <verify>
    <automated>python -m pytest tests/test_config.py -v 2>&1 | head -20</automated>
  </verify>
  <done>
    - Config tests exist
    - Profile selection tested
  </done>
</task>

<task type="auto">
  <name>Task 4: Create memory system unit tests</name>
  <files>tests/test_memory.py</files>
  <action>
    Create tests/test_memory.py:
    
    1. test_memory_manager_init
    2. test_save_conversation
    3. test_get_context
    4. test_save_fact
    5. test_get_preferences
    
    Use mocking for ChromaDB to avoid file system dependencies.
  </action>
  <verify>
    <automated>python -m pytest tests/test_memory.py -v 2>&1 | head -20</automated>
  </verify>
  <done>
    - Memory tests exist
    - CRUD operations tested
  </done>
</task>

<task type="auto">
  <name>Task 5: Create integration test</name>
  <files>tests/test_integration.py</files>
  <action>
    Create tests/test_integration.py:
    
    1. test_text_input_to_response_pipeline
       - Mock user input
       - Process through agent
       - Verify response returned
       - No exceptions raised
    
    2. test_agent_handles_empty_input
    3. test_agent_handles_error_gracefully
    
    Use heavy mocking for Ollama to avoid actual LLM calls.
  </action>
  <verify>
    <automated>python -m pytest tests/test_integration.py -v 2>&1 | head -20</automated>
  </verify>
  <done>
    - Integration test exists
    - Full pipeline tested with mocking
  </done>
</task>

<task type="auto">
  <name>Task 6: Run all tests and verify</name>
  <files>tests/</files>
  <action>
    Run full test suite:
    
    ```bash
    python -m pytest tests/ -v --tb=short
    ```
    
    Verify:
    - All tests pass
    - No import errors
    - Coverage report generated
  </action>
  <verify>
    <automated>python -m pytest tests/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All tests pass
    - Test suite complete
  </done>
</task>

</tasks>

<verification>
- [ ] tests/test_hardware.py exists and passes
- [ ] tests/test_config.py exists and passes
- [ ] tests/test_memory.py exists and passes
- [ ] tests/test_integration.py exists and passes
- [ ] All tests run without errors
</verification>

<success_criteria>
Test suite complete with unit tests for hardware, config, memory, and integration test for the full agent pipeline.
</success_criteria>

<output>
After completion, create .planning/phases/09-test-suite/09-01-SUMMARY.md
</output>
