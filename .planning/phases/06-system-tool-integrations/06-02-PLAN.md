---
phase: 06-system-tool-integrations
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: []
autonomous: true
requirements: [ST-05, ST-06]
user_setup:
  - service: google_calendar
    why: "Calendar API access"
    env_vars:
      - name: GOOGLE_CLIENT_SECRETS
        source: "Google Cloud Console -> Credentials -> OAuth 2.0 -> Download JSON"
    dashboard_config:
      - task: "Enable Google Calendar API"
        location: "Google Cloud Console -> APIs & Services -> Enable API"
      - task: "Enable Gmail API"
        location: "Google Cloud Console -> APIs & Services -> Enable API"
must_haves:
  truths:
    - "Google Calendar integration authenticates via OAuth2 and can CRUD events"
    - "Google Email integration authenticates via OAuth2 and can read/send emails"
    - "Tokens stored encrypted in data/ directory"
    - "Token refresh happens automatically"
  artifacts:
    - path: "tools/auth/__init__.py"
      provides: "Auth module package"
    - path: "tools/auth/token_manager.py"
      provides: "Encrypted token storage with Fernet"
      exports: ["TokenManager"]
    - path: "tools/auth/oauth.py"
      provides: "OAuth2 flow helpers (CLI-based)"
      exports: ["GoogleOAuth"]
    - path: "tools/google_calendar.py"
      provides: "ST-05: Google Calendar CRUD"
      exports: ["GoogleCalendarTool"]
    - path: "tools/google_email.py"
      provides: "ST-06: Google Email (Gmail) read/send"
      exports: ["GoogleEmailTool"]
  key_links:
    - from: "tools/google_calendar.py"
      to: "tools/auth/token_manager.py"
      via: "loads/saves encrypted tokens"
    - from: "tools/google_email.py"
      to: "tools/auth/oauth.py"
      via: "uses OAuth2 flow for authentication"
---
<objective>
Implement Google Calendar and Email integrations with OAuth2 authentication and encrypted token storage.
</objective>

<execution_context>
@C:/Users/ashut/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-system-tool-integrations/06-CONTEXT.md
@.planning/phases/06-system-tool-integrations/06-RESEARCH.md
@brain/tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth module with TokenManager</name>
  <files>tools/auth/__init__.py, tools/auth/token_manager.py</files>
  <action>
Create tools/auth/ directory with:
- token_manager.py: TokenManager class using cryptography.Fernet
  - save_credentials(provider, credentials) encrypts and saves to data/
  - load_credentials(provider) loads and decrypts
  - refresh_if_needed(credentials) auto-refreshes expired tokens
  - Uses access_type='offline' to get refresh tokens
- __init__.py exports TokenManager
Per user decision: File-based encrypted JSON, automatic token refresh.
  </action>
  <verify>TokenManager encrypts/decrypts: `python -c "from tools.auth import TokenManager; tm = TokenManager(); print('TokenManager ready')"`</verify>
  <done>Token storage encrypts/decrypts, auto-refresh works</done>
</task>

<task type="auto">
  <name>Task 2: Implement Google OAuth2 flow helper</name>
  <files>tools/auth/oauth.py</files>
  <action>
Create GoogleOAuth class:
- authenticate(scopes, client_secrets_path) prints URL, prompts for code
- Uses InstalledAppFlow.from_client_secrets_file()
- Sets access_type='offline' for refresh tokens
- Returns Credentials object
Per user decision: CLI-based (print URL, paste code).
  </action>
  <verify>OAuth flow exists: `python -c "from tools.auth.oauth import GoogleOAuth; print('GoogleOAuth ready')"`</verify>
  <done>OAuth2 CLI flow works, returns credentials</done>
</task>

<task type="auto">
  <name>Task 3: Implement Google Calendar tool (ST-05)</name>
  <files>tools/google_calendar.py</files>
  <action>
Implement GoogleCalendarTool:
- __init__(client_secrets_path) loads OAuth config
- authenticate() triggers OAuth flow if needed
- list_events(max_results=10) returns list of events
- create_event(title, start_time, end_time, description=None) creates event
- update_event(event_id, **updates) modifies event
- delete_event(event_id) removes event
- Uses google-api-python-client + google-auth-oauthlib
- Streams results via callback if provided
- Full logging, detailed errors with suggestions
Per user decision: Token storage, refresh, auth all per locked decisions.
  </action>
  <verify>Calendar tool imports: `python -c "from tools.google_calendar import GoogleCalendarTool; print('Calendar ready')"`</verify>
  <done>Calendar CRUD works with OAuth2 authentication</done>
</task>

<task type="auto">
  <name>Task 4: Implement Google Email tool (ST-06)</name>
  <files>tools/google_email.py</files>
  <action>
Implement GoogleEmailTool:
- __init__(client_secrets_path) loads OAuth config
- authenticate() triggers OAuth flow if needed
- list_messages(max_results=10, query=None) returns message list
- get_message(message_id) returns full message
- send_message(to, subject, body) sends email
- Uses Gmail API via google-api-python-client
- Streams results via callback if provided
- Full logging, detailed errors with suggestions
Per user decision: Same auth flow as Calendar (unified per provider).
  </action>
  <verify>Email tool imports: `python -c "from tools.google_email import GoogleEmailTool; print('Email ready')"`</verify>
  <done>Email read/send works with OAuth2 authentication</done>
</task>

</tasks>

<verification>
Google Calendar and Email tools authenticate via OAuth2, tokens encrypted and auto-refresh
</verification>

<success_criteria>
Calendar can CRUD events, Email can read/send, both use encrypted token storage with automatic refresh
</success_criteria>

<output>
After completion, create `.planning/phases/06-system-tool-integrations/06-02-SUMMARY.md`
</output>
