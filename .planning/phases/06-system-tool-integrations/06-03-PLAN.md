---
phase: 06-system-tool-integrations
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified: []
autonomous: true
requirements: [ST-07, ST-08, ST-09]
user_setup:
  - service: microsoft_outlook
    why: "Outlook/Exchange integration"
    env_vars:
      - name: MICROSOFT_CLIENT_ID
        source: "Azure Portal -> App Registrations -> Copy Application (client) ID"
    dashboard_config:
      - task: "Add API permissions"
        location: "Azure Portal -> App Registrations -> API Permissions -> Microsoft Graph -> Calendars, Mail"
      - task: "Configure redirect URI"
        location: "Azure Portal -> App Registrations -> Redirect URIs"
must_haves:
  truths:
    - "Microsoft Outlook integration authenticates and can read/send emails"
    - "System monitor returns CPU, memory, disk, network metrics"
    - "All tools registered in ToolRegistry with descriptions"
  artifacts:
    - path: "tools/auth/microsoft.py"
      provides: "Microsoft OAuth2 authentication"
      exports: ["MicrosoftAuth"]
    - path: "tools/microsoft_outlook.py"
      provides: "ST-07: Microsoft Outlook/Graph integration"
      exports: ["MicrosoftOutlookTool"]
    - path: "tools/system_monitor.py"
      provides: "ST-08: System diagnostics (CPU, memory, disk, network)"
      exports: ["SystemMonitorTool"]
    - path: "brain/tools.py"
      provides: "ST-09: ToolRegistry updated with all tools"
  key_links:
    - from: "brain/tools.py"
      to: "tools/browser.py"
      via: "registry.register('browser', ...)"
    - from: "brain/tools.py"
      to: "tools/filesystem.py"
      via: "registry.register('filesystem', ...)"
    - from: "brain/tools.py"
      to: "tools/google_calendar.py"
      via: "registry.register('google_calendar', ...)"
    - from: "brain/tools.py"
      to: "tools/google_email.py"
      via: "registry.register('google_email', ...)"
    - from: "brain/tools.py"
      to: "tools/microsoft_outlook.py"
      via: "registry.register('outlook', ...)"
    - from: "brain/tools.py"
      to: "tools/system_monitor.py"
      via: "registry.register('system_monitor', ...)"
    - from: "brain/tools.py"
      to: "tools/code_exec.py"
      via: "registry.register('execute_code', ...)"
    - from: "brain/tools.py"
      to: "tools/web_search.py"
      via: "registry.register('web_search', ...)"
---
<objective>
Implement Microsoft Outlook integration, system monitor tool, and register all tools in ToolRegistry.
</objective>

<execution_context>
@C:/Users/ashut/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-system-tool-integrations/06-CONTEXT.md
@.planning/phases/06-system-tool-integrations/06-RESEARCH.md
@brain/tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Microsoft OAuth2 authentication</name>
  <files>tools/auth/microsoft.py</files>
  <action>
Create MicrosoftAuth class using azure.identity.DeviceCodeCredential:
- Uses DeviceCodeCredential for CLI-based auth (prints code, user enters)
- get_client() returns GraphServiceClient from msgraph-sdk
- Handles token caching for persistence
- Uses offline_access scope for refresh tokens
Per user decision: CLI-based auth, file-based token storage, automatic refresh.
  </action>
  <verify>Microsoft auth exists: `python -c "from tools.auth.microsoft import MicrosoftAuth; print('MicrosoftAuth ready')"`</verify>
  <done>Microsoft OAuth2 works with CLI flow</done>
</task>

<task type="auto">
  <name>Task 2: Implement Microsoft Outlook tool (ST-07)</name>
  <files>tools/microsoft_outlook.py</files>
  <action>
Implement MicrosoftOutlookTool:
- __init__(client_id, scopes) initializes auth
- authenticate() triggers device code flow if needed
- list_emails(max_results=10) returns email list
- get_email(email_id) returns full email
- send_email(to, subject, body) sends email
- list_calendar_events(max_results=10) returns events
- Uses msgraph-sdk for Microsoft Graph API
- Streams results via callback if provided
- Full logging, detailed errors with suggestions
Per user decision: Same auth/token patterns as Google.
  </action>
  <verify>Outlook tool imports: `python -c "from tools.microsoft_outlook import MicrosoftOutlookTool; print('Outlook ready')"`</verify>
  <done>Outlook email read/send works with Microsoft Graph</done>
</task>

<task type="auto">
  <name>Task 3: Implement System Monitor tool (ST-08)</name>
  <files>tools/system_monitor.py</files>
  <action>
Implement SystemMonitorTool using psutil (already in requirements):
- get_cpu_usage() returns CPU percentage
- get_memory_usage() returns {total, available, used, percent}
- get_disk_usage(path='C:\\') returns disk stats
- get_network_stats() returns {bytes_sent, bytes_recv, packets}
- get_all() returns comprehensive system stats
- Async support with streaming callback
- Full logging, detailed errors with suggestions
Per user decision: Streaming + async, full logging, detailed errors.
  </action>
  <verify>System monitor works: `python -c "from tools.system_monitor import SystemMonitorTool; s = SystemMonitorTool(); print(s.get_cpu_usage())"`</verify>
  <done>System monitor returns CPU, memory, disk, network metrics</done>
</task>

<task type="auto">
  <name>Task 4: Register all tools in ToolRegistry (ST-09)</name>
  <files>brain/tools.py</files>
  <action>
Update brain/tools.py create_default_registry() to import and register all tools:
- Import all tool classes from tools package
- Register with descriptive strings:
  - browser: "Navigate to URLs, extract content, fill forms, click elements"
  - filesystem: "Read, write, delete files. Requires user confirmation."
  - google_calendar: "List, create, update Google Calendar events"
  - google_email: "Read, send Google Email messages"
  - outlook: "Read, send Microsoft Outlook emails"
  - system_monitor: "Get CPU, memory, disk, network statistics"
  - execute_code: "Run Python code in sandboxed environment"
  - web_search: "Search the web using browser automation"
- Each tool registered with execute method as callable
- Add create_tools_registry() factory function
  </action>
  <verify>All tools registered: `python -c "from brain.tools import create_tools_registry; r = create_tools_registry(); print(r.list_tools())"`</verify>
  <done>All 8 tools registered in ToolRegistry with descriptions</done>
</task>

</tasks>

<verification>
Microsoft Outlook works, system monitor returns metrics, all tools registered in ToolRegistry
</verification>

<success_criteria>
ST-07: Outlook can read/send emails, ST-08: System metrics returned, ST-09: All tools in registry
</success_criteria>

<output>
After completion, create `.planning/phases/06-system-tool-integrations/06-03-SUMMARY.md`
</output>
