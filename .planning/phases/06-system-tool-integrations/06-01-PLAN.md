---
phase: 06-system-tool-integrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [ST-01, ST-02, ST-03, ST-04]
user_setup: []
must_haves:
  truths:
    - "Web search returns relevant results from search queries"
    - "Browser automation can open pages, extract content, and interact with elements"
    - "Filesystem tool can read, write, and delete files with path expansion"
    - "Code execution runs Python code in sandboxed environment with limits"
  artifacts:
    - path: "tools/__init__.py"
      provides: "Tool module package"
    - path: "tools/web_search.py"
      provides: "ST-01: Web search functionality"
      exports: ["WebSearchTool"]
    - path: "tools/browser.py"
      provides: "ST-02: Browser automation with Playwright"
      exports: ["BrowserTool", "BrowserManager"]
    - path: "tools/filesystem.py"
      provides: "ST-03: Filesystem operations with ~ expansion"
      exports: ["FilesystemTool"]
    - path: "tools/code_exec.py"
      provides: "ST-04: Sandboxed code execution"
      exports: ["CodeExecutionTool"]
  key_links:
    - from: "tools/web_search.py"
      to: "tools/browser.py"
      via: "uses BrowserTool for search results"
    - from: "tools/filesystem.py"
      to: "brain/tools.py"
      via: "registered as tool in ToolRegistry"
---

<objective>
Implement core system tools: web search, browser automation, filesystem manipulation, and sandboxed code execution.
</objective>

<execution_context>
@C:/Users/ashut/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-system-tool-integrations/06-CONTEXT.md
@.planning/phases/06-system-tool-integrations/06-RESEARCH.md
@brain/tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tools/ directory structure and base classes</name>
  <files>tools/__init__.py, tools/base.py</files>
  <action>
Create tools/ directory with __init__.py exporting all tools. Create tools/base.py with:
- BaseTool abstract class with execute() method
- ToolError exception with tool_name, message, suggestion fields
- execute_with_error_handling() wrapper for consistent error handling
All tools inherit from BaseTool.
  </action>
  <verify>tools/ imports work: `python -c "from tools import WebSearchTool, BrowserTool, FilesystemTool, CodeExecutionTool"`</verify>
  <done>Base tool infrastructure exists, all tool classes importable</done>
</task>

<task type="auto">
  <name>Task 2: Implement Browser automation tool (ST-02)</name>
  <files>tools/browser.py</files>
  <action>
Implement BrowserTool class using Playwright with:
- __init__(user_data_dir=None) for persistent sessions
- launch() launches Chromium with headless=False (visible window per user decision)
- navigate(url) navigates to URL
- extract(selector=None) extracts page content or element text
- search(query) uses DuckDuckGo for web search, returns results
- Uses persistent context for session persistence
- Full logging via loguru to file + console
- Detailed error messages with suggestions (e.g., "Install browser: playwright install chromium")
Per user decision: persistent sessions, visible window, text extraction with highlighting.
  </action>
  <verify>Browser launches: `python -c "from tools.browser import BrowserTool; b = BrowserTool(); b.launch(); print('Browser ready')"`</verify>
  <done>Browser automation works with persistent context, visible window, search returns results</done>
</task>

<task type="auto">
  <name>Task 3: Implement Web Search tool (ST-01)</name>
  <files>tools/web_search.py</files>
  <action>
Implement WebSearchTool wrapping BrowserTool.search():
- search(query, num_results=5) returns list of {title, url, snippet}
- Highlights "important details" in results per user decision
- Async support with streaming results callback
- Full logging to file + console
- Detailed error handling with suggestions
  </action>
  <verify>Web search returns results: `python -c "from tools.web_search import WebSearchTool; s = WebSearchTool(); print(s.search('test'))"`</verify>
  <done>Web search returns relevant results with titles, URLs, snippets</done>
</task>

<task type="auto">
  <name>Task 4: Implement Filesystem tool (ST-03)</name>
  <files>tools/filesystem.py</files>
  <action>
Implement FilesystemTool with:
- expand_path(path) expands ~ to user home on Windows
- read_file(path) reads file content
- write_file(path, content) writes with user confirmation (raises confirmation request)
- delete_file(path) deletes with user confirmation
- All methods raise confirmation request for write/delete per user decision
- Path validation to prevent traversal attacks
- Full logging, detailed errors with suggestions
Per user decision: User confirms each write/delete, expand ~ to home.
  </action>
  <verify>Path expansion works: `python -c "from tools.filesystem import FilesystemTool; f = FilesystemTool(); print(f.expand_path('~/test'))"`</verify>
  <done>Filesystem tool reads/writes/deletes with confirmation, ~ expands correctly</done>
</task>

<task type="auto">
  <name>Task 5: Implement Code Execution tool (ST-04)</name>
  <files>tools/code_exec.py</files>
  <action>
Implement CodeExecutionTool using safe-py-runner:
- __init__(timeout=30, memory_mb=128)
- execute(code) runs Python in sandbox
- Blocks: os, subprocess, socket, requests, importlib, etc.
- Returns {output, error, status} dict
- Streaming output via callback if provided
- Full logging, detailed errors with suggestions
Per user decision: streaming + async callbacks, full logging, detailed errors.
  </action>
  <verify>Code execution works: `python -c "from tools.code_exec import CodeExecutionTool; c = CodeExecutionTool(); print(c.execute('print(1+1)'))"`</verify>
  <done>Sandboxed code executes with limits, blocked imports prevented</done>
</task>

</tasks>

<verification>
All core tools importable from tools package and function correctly
</verification>

<success_criteria>
Web search returns results, browser automation works with persistent sessions, filesystem handles ~ expansion with confirmations, code execution runs sandboxed
</success_criteria>

<output>
After completion, create `.planning/phases/06-system-tool-integrations/06-01-SUMMARY.md`
</output>
