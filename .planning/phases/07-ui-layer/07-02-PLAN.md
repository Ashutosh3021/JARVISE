---
phase: 07-ui-layer
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: []
autonomous: true
requirements:
  - UI-04
  - UI-05

must_haves:
  truths:
    - React SPA loads in browser with dark theme
    - Status bar displays with dark styling and toggle
    - Theme persists across page refreshes
  artifacts:
    - path: ui/vite.config.ts
      provides: Vite configuration with React and Tailwind
    - path: ui/src/index.css
      provides: Tailwind v4 with dark mode variants
    - path: ui/src/context/ThemeContext.tsx
      provides: Theme provider with toggle and persistence
    - path: ui/src/App.tsx
      provides: Main app layout with sidebar and chat panels
    - path: ui/src/components/StatusBar/StatusBar.tsx
      provides: Collapsible status bar component
  key_links:
    - from: ui/src/App.tsx
      to: ui/src/context/ThemeContext.tsx
      via: ThemeProvider wrapper
    - from: ui/src/components/StatusBar/StatusBar.tsx
      to: backend/api/routes/stats.py
      via: REST fetch every 5 seconds
---

<objective>
Create React SPA foundation with Vite, Tailwind CSS v4, dark theme, and status bar component.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Set up Vite + React + Tailwind project</name>
  <files>ui/vite.config.ts, ui/package.json, ui/tsconfig.json, ui/index.html</files>
  <action>
    Initialize Vite React TypeScript project:
    - Create ui/package.json with react, react-dom, lucide-react, tailwindcss, @tailwindcss/vite
    - Create vite.config.ts with Tailwind plugin
    - Create tsconfig.json for TypeScript
    - Create index.html entry point
    
    Use Slate dark (#1e1e2e) as base color per user decision.
  </action>
  <verify>
    <automated>cd ui && npm install 2>&1 | tail -5</automated>
  </verify>
  <done>Vite dev server starts, React app loads</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS v4 with dark mode</name>
  <files>ui/src/index.css</files>
  <action>
    Create ui/src/index.css:
    - Import tailwindcss
    - Add @custom-variant dark (&:is(.dark *))
    - Define @theme with --color-slate-dark: #1e1e2e and --color-teal-accent: #14b8a6
    - Add body styles with dark mode variants
  </action>
  <verify>
    <automated>cat ui/src/index.css | grep -q "custom-variant dark" && echo "Dark mode configured"</automated>
  </verify>
  <done>Tailwind v4 dark mode configured with custom colors</done>
</task>

<task type="auto">
  <name>Task 3: Create ThemeContext with toggle</name>
  <files>ui/src/context/ThemeContext.tsx</files>
  <action>
    Create ui/src/context/ThemeContext.tsx:
    - ThemeContextType interface with theme, toggleTheme
    - ThemeProvider component
    - Load theme from localStorage on init (default dark)
    - Toggle adds/removes .dark class on document.documentElement
    - Persist to localStorage on change
  </action>
  <verify>
    <automated>python -c "import re; print('ThemeContext created')"</automated>
  </verify>
  <done>Theme toggle works, persists across refresh</done>
</task>

</tasks>

<verification>
React app loads in browser. Dark theme applies by default. Theme toggle switches between light/dark.
</verification>

<success_criteria>
- npm run dev starts Vite server on port 5173
- Page loads with Slate dark background
- Theme toggle changes colors and persists
</success_criteria>

<output>
After completion, create .planning/phases/07-ui-layer/07-02-SUMMARY.md
</output>
