---
phase: 07-ui-layer
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/routes/stats.py
  - ui/src/components/StatusBar/StatusBar.tsx
  - ui/src/components/Chat/ChatWindow.tsx
  - ui/src/components/Waveform/Waveform.tsx
autonomous: true
gap_closure: true
requirements:
  - UI-06
  - UI-08
must_haves:
  truths:
    - "User sees live system stats (CPU, RAM, VRAM) from backend API"
    - "User sees animated waveform during JARVIS thinking state"
  artifacts:
    - path: "backend/api/routes/stats.py"
      provides: "GET /api/stats/current endpoint"
      exports: ["GET /api/stats/current"]
    - path: "ui/src/components/Waveform/Waveform.tsx"
      provides: "Animated waveform visualization"
      exports: ["Waveform component"]
  key_links:
    - from: "StatusBar.tsx"
      to: "/api/stats/current"
      via: "fetch in useEffect"
      pattern: "fetch.*api/stats/current"
    - from: "ChatWindow.tsx"
      to: "Waveform.tsx"
      via: "import and use"
      pattern: "import.*Waveform"
---

<objective>
Close two verification gaps from 07-VERIFICATION.md:
1. UI-08: Stats API endpoint missing - StatusBar fetches from /api/stats/current but endpoint doesn't exist
2. UI-06: Animated waveform not implemented - only bouncing dots exist for thinking state
</objective>

<context>
@tools/system_monitor.py - SystemMonitorTool provides get_all() method returning {cpu, memory, disk, network} stats
@ui/src/components/StatusBar/StatusBar.tsx - Line 21 fetches /api/stats/current, needs real endpoint
@ui/src/components/Chat/ChatWindow.tsx - Lines 42-55 show bouncing dots, need waveform instead

Backend routes pattern:
- backend/api/routes/memory.py registered at /api prefix
- backend/api/routes/chat.py registered without prefix (WebSocket)
- New stats.py should follow memory.py pattern with /api prefix
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Stats API endpoint</name>
  <files>backend/api/routes/stats.py</files>
  <action>
Create GET /api/stats/current endpoint in backend/api/routes/stats.py:
- Import SystemMonitorTool from tools.system_monitor
- Create FastAPI router with @router.get("/stats/current")
- Instantiate SystemMonitorTool() and call get_all()
- Return simplified {cpu, memory, vram?} format matching StatusBar.tsx expectations:
  - cpu: {percent: float}
  - memory: {percent: float, used_gb: float, total_gb: float}
  - vram: Optional[{percent: float, used_gb: float, total_gb: float}] - can be null for now
- Register router in backend/main.py with: app.include_router(stats.router, prefix="/api", tags=["stats"])
- Note: VRAM requires nvidia-ml-py, handle gracefully if unavailable
  </action>
  <verify>
Start backend server and curl http://localhost:8000/api/stats/current returns valid JSON with cpu/memory fields
  </verify>
  <done>GET /api/stats/current returns real system stats (CPU %, memory %, and optional VRAM)</done>
</task>

<task type="auto">
  <name>Task 2: Create Waveform component</name>
  <files>ui/src/components/Waveform/Waveform.tsx</files>
  <action>
Create animated waveform visualization component at ui/src/components/Waveform/Waveform.tsx:
- Accept isAnimating prop to control animation state
- Use CSS animations with multiple bars (5-7 bars) that animate at different frequencies
- Style: Teal accent color (#14b8a6), bars with rounded tops
- Animation: Bars scale Y with different delays creating wave effect
- Container: Match bubble styling from ChatWindow (bg-slate-700, rounded-2xl)
- Dimensions: ~60px wide, ~24px tall bars
- Use CSS keyframes (no external library needed per CONTEXT.md "thinking state: Animated dots")
  </action>
  <verify>
Waveform.tsx exports Waveform component that renders animated bars when isAnimating=true
  </verify>
  <done>Waveform component renders animated bars in bubble container</done>
</task>

<task type="auto">
  <name>Task 3: Update ChatWindow to use Waveform</name>
  <files>ui/src/components/Chat/ChatWindow.tsx</files>
  <action>
Replace bouncing dots (lines 47-52) with Waveform component:
- Import Waveform from ./Waveform
- Replace the bouncing dots div with: &lt;Waveform isAnimating={isThinking} /&gt;
- Keep the avatar and bubble container structure
  </action>
  <verify>
ChatWindow shows animated waveform instead of bouncing dots when isThinking=true
  </verify>
  <done>UI-06 satisfied: Animated waveform displays during thinking state</done>
</task>

</tasks>

<verification>
- [ ] GET /api/stats/current returns real CPU/memory data (not placeholder)
- [ ] StatusBar shows connected=true when backend available
- [ ] ChatWindow displays animated waveform (not bouncing dots) during thinking
- [ ] Both gaps from 07-VERIFICATION.md resolved
</verification>

<success_criteria>
Gap closure complete when:
- StatusBar receives real stats from /api/stats/current (not placeholder data)
- ChatWindow shows animated waveform visualization during thinking state
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-layer/07-04-SUMMARY.md`
</output>
