---
phase: 07-ui-layer
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified: []
autonomous: true
requirements:
  - UI-06
  - UI-07
  - UI-08

must_haves:
  truths:
    - Chat window displays messages with user right, JARVIS left
    - Streaming tokens appear word-by-word (chunk reveal)
    - System stats display and update every 5 seconds
    - Animated dots show when JARVIS is thinking
  artifacts:
    - path: ui/src/components/Chat/ChatWindow.tsx
      provides: Chat bubble interface with streaming
    - path: ui/src/components/Chat/Message.tsx
      provides: Individual message bubble component
    - path: ui/src/components/Sidebar/Sidebar.tsx
      provides: Collapsible navigation sidebar
    - path: ui/src/components/Toolbar/Toolbar.tsx
      provides: Input toolbar with text, voice, file, emoji
    - path: ui/src/components/Waveform/Waveform.tsx
      provides: Animated waveform display
    - path: ui/src/hooks/useChatWebSocket.ts
      provides: WebSocket hook for chat streaming
    - path: ui/src/hooks/useSystemStats.ts
      provides: Hook for polling system stats every 5s
  key_links:
    - from: ui/src/components/Chat/ChatWindow.tsx
      to: ui/src/hooks/useChatWebSocket.ts
      via: useChatWebSocket('ws://localhost:8000/ws/chat')
    - from: ui/src/components/StatusBar/StatusBar.tsx
      to: ui/src/hooks/useSystemStats.ts
      via: useSystemStats(5000)
---

<objective>
Create chat interface with streaming tokens, sidebar navigation, toolbar, waveform display, and live system stats.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create WebSocket hooks for chat and system stats</name>
  <files>ui/src/hooks/useChatWebSocket.ts, ui/src/hooks/useSystemStats.ts</files>
  <action>
    Create useChatWebSocket.ts:
    - Connect to ws://localhost:8000/ws/chat
    - Handle token messages: append to last assistant message
    - Handle done: mark streaming complete
    - Send message: add user message, create streaming placeholder
    - Include connectionStatus state
    
    Create useSystemStats.ts:
    - Fetch from /api/stats/current every 5 seconds
    - Return SystemStats interface with cpu, memory, vram
    - Handle null gracefully for missing VRAM
  </action>
  <verify>
    <automated>python -c "import os; print('Hooks created')"</automated>
  </verify>
  <done>WebSocket connects, tokens stream, stats poll</done>
</task>

<task type="auto">
  <name>Task 2: Create chat components (ChatWindow, Message)</name>
  <files>ui/src/components/Chat/ChatWindow.tsx, ui/src/components/Chat/Message.tsx</files>
  <action>
    Create Message.tsx:
    - Props: role (user/assistant), content, isStreaming
    - User: right-aligned, teal background (#14b8a6)
    - JARVIS: left-aligned, slate-700 background
    - Show cursor animation during streaming
    
    Create ChatWindow.tsx:
    - Use useChatWebSocket hook
    - Map messages to Message components
    - Show animated dots (three bouncing dots) when thinking
    - Auto-scroll to bottom on new messages
  </action>
  <verify>
    <automated>python -c "import os; print('Chat components created')"</automated>
  </verify>
  <done>Messages display in bubbles, streaming works</done>
</task>

<task type="auto">
  <name>Task 3: Create Sidebar, Toolbar, and Waveform</name>
  <files>ui/src/components/Sidebar/Sidebar.tsx, ui/src/components/Toolbar/Toolbar.tsx, ui/src/components/Waveform/Waveform.tsx</files>
  <action>
    Create Sidebar.tsx:
    - Icons from lucide-react (Home, MessageSquare, Settings, etc.)
    - Collapsible to maximize chat area
    - Teal accent on active state
    
    Create Toolbar.tsx:
    - Text input field
    - Voice button, file upload button, emoji button
    - Teal accent color
    
    Create Waveform.tsx:
    - CSS animation or canvas for voice activity
    - Animated bars responding to audio levels
  </action>
  <verify>
    <automated>python -c "import os; print('UI components created')"</automated>
  </verify>
  <done>All components render with proper styling</done>
</task>

</tasks>

<verification>
Chat messages appear correctly. Tokens stream word-by-word. System stats update every 5 seconds. Waveform animates.
</verification>

<success_criteria>
- Chat sends/receives messages via WebSocket
- Streaming shows chunk-by-chunk reveal
- Status bar shows CPU/Memory/VRAM with 5s updates
- Sidebar collapses, toolbar functional
</success_criteria>

<output>
After completion, create .planning/phases/07-ui-layer/07-03-SUMMARY.md
</output>
