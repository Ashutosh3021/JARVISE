---
phase: 07-ui-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - FastAPI server starts with WebSocket support
    - Token streaming works from brain/agent.py to WebSocket client
    - Memory management endpoints are accessible via REST
  artifacts:
    - path: backend/main.py
      provides: FastAPI app with CORS and WebSocket support
    - path: backend/api/routes/chat.py
      provides: WebSocket endpoint for token streaming
    - path: backend/api/routes/memory.py
      provides: REST endpoints for memory CRUD
    - path: backend/api/websocket/manager.py
      provides: WebSocket connection manager
  key_links:
    - from: backend/api/routes/chat.py
      to: brain/agent.py
      via: ReActAgent.stream_run()
      pattern: agent\\.stream_run
---

<objective>
Create FastAPI backend with WebSocket support for live token streaming and memory management REST endpoints.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create FastAPI server with WebSocket manager</name>
  <files>backend/main.py, backend/api/websocket/manager.py</files>
  <action>
    Create backend/main.py with:
    - FastAPI app with CORS middleware (allow http://localhost:5173 for Vite dev)
    - Lifespan context for startup/shutdown
    - Import routers from api.routes
    
    Create backend/api/websocket/manager.py with:
    - ConnectionManager class with connect/disconnect/send_token methods
    - List of active WebSocket connections
  </action>
  <verify>
    <automated>python -c "from backend.main import app; print('FastAPI app loads')"</automated>
  </verify>
  <done>FastAPI server starts, CORS configured for localhost:5173</done>
</task>

<task type="auto">
  <name>Task 2: Create WebSocket chat streaming endpoint</name>
  <files>backend/api/routes/chat.py</files>
  <action>
    Create backend/api/routes/chat.py with:
    - WebSocket endpoint at /ws/chat
    - Connect to ConnectionManager
    - Receive message JSON, extract "message" field
    - Call brain/agent.py stream_run() method
    - Send each token as {"type": "token", "content": token}
    - Send {"type": "done"} when complete
    - Handle WebSocketDisconnect gracefully
  </action>
  <verify>
    <automated>python -c "from backend.api.routes.chat import router; print('Chat router loads')"</automated>
  </verify>
  <done>WebSocket /ws/chat streams tokens from agent</done>
</task>

<task type="auto">
  <name>Task 3: Create memory management REST endpoints</name>
  <files>backend/api/routes/memory.py</files>
  <action>
    Create backend/api/routes/memory.py with REST endpoints:
    - GET /api/memory - List memories (from ChromaDB)
    - GET /api/memory/{id} - Get specific memory
    - POST /api/memory - Create new memory entry
    - DELETE /api/memory/{id} - Delete memory
    - GET /api/memory.md - Read MEMORY.md file content
    - PUT /api/memory.md - Write MEMORY.md file
    
    These will connect to memory/ module when available.
  </action>
  <verify>
    <automated>python -c "from backend.api.routes.memory import router; print('Memory router loads')"</automated>
  </verify>
  <done>REST endpoints for memory CRUD operations exist</done>
</task>

</tasks>

<verification>
FastAPI server starts with uvicorn. WebSocket connects and receives streamed tokens. Memory endpoints return valid JSON responses.
</verification>

<success_criteria>
- FastAPI server starts without errors
- WebSocket endpoint accepts connections
- Token streaming sends tokens to client
- Memory endpoints are accessible (may return empty if memory not initialized)
</success_criteria>

<output>
After completion, create .planning/phases/07-ui-layer/07-01-SUMMARY.md
</output>
